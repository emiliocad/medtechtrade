<?php 
    $r=$this->thread;
?>
<h1>Thread [Edit]</h1>
<br /><br /><br />
<?php if(isset($this->errors)):?>
    <?php $e = $this->errors;?>
<div id="errors">
    Please correct the errors. 
</div>
<?php endif;?>
<form method="post" action="admin/forum/edit-thread">
    <input type="hidden" name="id" value="<?php echo $r['id'];?>" />
    <div class="field">
    	<label>Title</label>
    	<input type="text" name="title" value="<?php echo (isset($r['title']))? $r['title'] : ""?>" />
    	<span class="explanation"></span>
    </div>
    <div class="field">
    	<label>Forum</label>
    	<select name="forum_id">
            <option value="0">Select One...</option>
            <?php foreach ($this->forums as $key=>$forum):?>
            <option value="<?php echo $key;?>" <?php if(isset($r['forum_id']) && $key==$r['forum_id']) echo 'selected="selected"';?>><?php echo $forum;?></option>
            <?php endforeach;?>
        </select>
    	<span class="explanation"></span>
    </div>
    <div class="field">
    	<label>Owner</label>
    	<select name="account_id">
            <option value="0">Select One...</option>
            <?php foreach ($this->authors as $key=>$author):?>
            <option value="<?php echo $key;?>" <?php if(isset($r['account_id']) && $key==$r['account_id']) echo 'selected="selected"';?>><?php echo $author;?></option>
            <?php endforeach;?>
        </select>
    	<span class="explanation"></span>
    </div>
    <div class="field">
    	<label>Description</label>
    	<input type="text" name="description" value="<?php echo (isset($r['description']))? $r['description'] : ""?>"" />
    	<span class="explanation"></span>
    </div>
    <div class="field">
    	<label>Tags</label>
    	<input type="text" name="tags" value="<?php echo (isset($r['tags']))? $r['tags'] : ""?>"" />
    	<span class="explanation"></span>
    </div>
    <div class="field">
    	<label>Sticky thread</label>
    	<input type="checkbox" name="sticky" value="1" <?php if($r['sticky']) echo 'checked="checked"'?>"" />
    	<span class="explanation"></span>
    </div>
    
    <div class="field">
    	<label>Creation Date</label>
    	<input type="text" name="creation_date" value="<?php echo $r['creation_date']?>" readonly="readonly"/>
    	<span class="explanation"></span>
    </div>
    <input type="submit" name="save" value="Save" />
</form>